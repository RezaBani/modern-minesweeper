export struct Position {
    row:int,
    col:int,
}

export struct Tile {
    value: int, // -1 indicate there is a bomb
    visible: bool,
    flagged: bool,
}

export enum GameState {
    Initial,
    Normal,
    Lose,
    Win
}
